# AGENTS — local (Codex)

Этот файл дополняет корневой `AGENTS.md`.
Сначала прочитай `../AGENTS.md` или `../../AGENTS.md` (см. ссылку ниже).

Ссылка на корень: ../../AGENTS.md

Локальный контекст:
Пример Rust‑проекта для индексации. Есть `Cargo.toml` и `src/`.
Для локальных прогонов помни: `code-indexer index` теперь инкрементальный по `content_hash`; для полного rebuild удаляй `.code-index.db`.
